version: '3.8'

services:
  stealth-browser:
    build: .
    ports:
      - "9222:9222"
    restart: unless-stopped
    # Optional: limit resources
    deploy:
      resources:
        limits:
          memory: 2G
          cpus: '1.0'
